# ACM Extended Fork Checkpoint — Phase 100

This checkpoint extends the Phase 85 fork through the remaining high-value durable intervention/provider state and introduces an executable offline config/source build gate.

## Phases 86-99

- EMMA patient attachment/provenance, BVM ownership, provider capture state and last-contact tuple now have explicit mutation boundaries.
- Prepared IV sets and used-bag provider inventories now have explicit mutation boundaries.
- AAJT-S site/timing/tracked-leg state commits through one owner. Device-owned tourniquets are snapshotted before AAJT state is cleared on removal.
- Blood-cooler coolant state and surgical-casualty disposition now have explicit writers.
- Active head-elevation state and paired XStat surgery/impairment state now have explicit writers.
- Thoracostomy output start time and the Phase 85 output totals/rate/history restore through their owners during clinical deserialization.
- ETT placement/complication and episode-provenance tuples now have explicit writers, including persistence restore.

## Phase 100 offline build gate

`tools/test_fork_phase100_offline_build_gate.py` validates:

- all addon `$PBOPREFIX$` roots;
- recursive repository-local `#include` resolution with exact-case paths;
- internal `\\x\\ACM\\addons\\...` virtual include targets while allowing external Arma/CBA/ACE include roots;
- the ACME CfgFunctions file-root contract;
- every empty ACME CfgFunctions registration against its exact `functions/fn_<name>.sqf` source file;
- repository-owned config `preprocessFileLineNumbers` targets.

Current gate result: **PASS — 13 addons, 111 local preprocessor files, 1,032 ACME functions**.

## Validation

- **95/95 fork phase regression tests pass** through Phase 100 using bounded/in-process execution.
- Whole-tree delimiter screening passes across **1,504 SQF files and 13 `config.cpp` files**.
- Python tools compile successfully; generated bytecode/cache files are removed before packaging.
- Direct ACM/ACE publication and runtime function-pointer monkey-patch guards remain intact from earlier phases.

## Validation boundary

A real HEMTT/PBO build is still pending. The current execution environment could inspect the release metadata but could not stage the HEMTT binary because outbound DNS/binary asset download was unavailable. The Phase 100 gate is intentionally an offline source/config/preprocessor contract check and does not claim HEMTT config merge, PBO packing/signing, Arma engine loading, dedicated-server/headless-client or multiplayer acceptance.

## Canonical full-source artifact

`ACM_Extended_Fork_Dev_Phase100_Source.zip`

- Size: **224,984,517 bytes**
- SHA-256: `4eb464a24c511ab2d7c44c7df9b39628081027ff603387533535654e42c17c54`

The full ZIP is the canonical recoverable Phase 100 source. This GitHub checkpoint documents the source state and validation results; it does not imply that the binary-heavy full source tree was pushed through the connector.
